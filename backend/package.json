{
  "name": "@tententen/backend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "build": "echo 'build placeholder'",
    "clean": "rimraf node_modules",
    "test": "echo 'no tests yet'",
    "type-check": "echo 'no TypeScript in backend yet'",
    "lint": "echo 'no linting configured yet'",
    "lint:fix": "echo 'no linting configured yet'",
    "format": "echo 'no formatting configured yet'",
    "format:check": "echo 'no formatting configured yet'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
